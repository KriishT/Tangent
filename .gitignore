# dependencies
node_modules
# build output
dist
# rust
src-tauri/target
# misc
.DS_Store
*.log
.env
.env.local
.env.*.local

# whisper models (downloaded at build — too large for GitHub)
models/*.bin
models/*.gguf
src-tauri/resources/models/*.bin

# updater signing keys (never commit private key)
keys/
*.key

